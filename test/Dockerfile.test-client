# Dockerfile for test client
FROM ubuntu:22.04

# Install dependencies
RUN apt-get update && \
    apt-get install -y \
    socat \
    netcat-openbsd \
    curl \
    iputils-ping \
    && rm -rf /var/lib/apt/lists/*

CMD ["/bin/bash", "-c", "sleep infinity"]
